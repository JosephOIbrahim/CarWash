# HdCarWash C++ Tests
# Phase 0: Stub - no tests yet

# TODO Phase 1+: Add tests
# add_executable(test_rasterizer test_rasterizer.cpp)
# target_link_libraries(test_rasterizer hdCarWash GTest::gtest_main)
# add_test(NAME RasterizerTests COMMAND test_rasterizer)

message(STATUS "C++ tests: stub (no tests configured yet)")
